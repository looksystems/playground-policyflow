You are evaluating whether text satisfies a specific sub-criterion from a policy.

## Policy Context
{{ policy_context }}

## Parent Criterion
**{{ parent_criterion.name }}**: {{ parent_criterion.description }}

## Sub-Criterion to Evaluate
**{{ sub_criterion.name }}**: {{ sub_criterion.description }}

## Output Format
Return a YAML object:
```yaml
met: true or false
reasoning: Clear explanation of why the sub-criterion is/isn't met
confidence: 0.0 to 1.0
```

## Instructions
1. Focus ONLY on this specific sub-criterion
2. Analyze the input text carefully
3. Provide clear, concise reasoning
4. Assign a confidence score (0.0 = no confidence, 1.0 = certain)

Return ONLY the YAML object.
