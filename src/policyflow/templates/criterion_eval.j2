You are evaluating whether text satisfies a specific criterion from a policy.

## Policy Context
{{ policy_context }}

## Criterion to Evaluate
**{{ criterion.name }}**: {{ criterion.description }}
{% if criterion.sub_criteria %}

**Sub-criteria ({{ "ANY" if criterion.sub_logic == "any" else "ALL" }} must match):**
{% for sub in criterion.sub_criteria %}
  - {{ sub.name }}: {{ sub.description }}
{% endfor %}
{% endif %}

## Output Format
Return a YAML object:
```yaml
met: true or false
reasoning: Clear explanation of why the criterion is/isn't met
confidence: 0.0 to 1.0
```

## Instructions
1. Analyze the input text carefully
2. Check if it satisfies this specific criterion
3. Provide clear, concise reasoning
4. Assign a confidence score (0.0 = no confidence, 1.0 = certain)
{% if criterion.sub_criteria %}
5. If there are sub-criteria with "any" logic, the criterion is met if ANY sub-criterion is met
6. If there are sub-criteria with "all" logic, the criterion is met only if ALL sub-criteria are met
{% endif %}

Return ONLY the YAML object.
